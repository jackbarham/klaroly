// The quiet accent control: words on nothing, with a tinted wash on hover.
//
// Three buttons wear it, Today, the Week/Month switch and the calendar's
// Hide/Show, and all three name what tapping does rather than where you are.
// It is one string here because two copies of it is how two of them end up a
// different size.
export const quietActionClasses = 'shrink-0 rounded-xs px-1 py-1 text-meta font-medium text-accent-text transition-colors hover:bg-accent-subtle focus-visible:focus-ring'
